---
description: Explore codebase — structure, dependencies, or deep dive into a concept
argument-hint: "<target>"
---
Use the Explore subagent to investigate `$ARGUMENTS`. 

Choose the strategy based on the target:
- **breadth**: for directories, project structure, entry points, config — surface-level scan
- **depth**: for a specific concept, domain, or pattern — deep read of key files

Tell the user which strategy you're using, then spawn the Explore agent with the appropriate strategy and scope.
